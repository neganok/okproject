version: v1.0
name: Apple Pipeline Example

agent:
  machine:
    type: a2-standard-4
    os_image: "macOS Xcode16"

blocks:
  - name: Build and Test iOS App
    task:
      jobs:
        - name: Run Xcode Build
          commands:
            - xcodebuild -version
            - echo "Your macOS environment with Xcode16 is ready!"
