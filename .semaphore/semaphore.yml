version: v1.0
name: Build Docker

agent:
  machine:
    type: e1-standard-2  # Chạy trên máy chủ Semaphore

blocks:
  - name: "🚀 Build & Run Docker"
    task:
      jobs:
        - name: "Build & Run"
          commands:
            - echo "Bắt đầu build Docker..."
            - docker build -t myapp .
            - docker run -d --name myapp-container myapp
            - echo "Container đã chạy thành công!"
